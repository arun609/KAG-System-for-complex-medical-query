✅ HIGH-LEVEL PHASES

Knowledge Graph Setup (PrimeKG Extraction → Neo4j Import → Verification)

Mutual Indexing Layer (Node Embeddings + Text Chunk Embeddings + Vector Store)

KAG Pipeline Development (Planner → Retrieval → Multi-hop Reasoning → Verifier)

Explainability + UI + Provenance Layer

Evaluation (Benchmarks + Clinician-style Cases)

Final Report + Slides + Demo Preparation

📅 DETAILED ROADMAP — FINISH BY JAN 15
WEEK 1 — Dec 15 to Dec 22
⭐ Goal: PrimeKG fully ingested + KG operational
Tasks
1. Download & Inspect PrimeKG

Obtain CSVs (nodes, edges, metadata).

Understand node types, relations, and source versions.

2. Neo4j Setup

Install Neo4j Desktop OR Neo4j Aura cloud.

Create database schema:

Node labels: Disease, Drug, Gene, Protein, Phenotype, Pathway.

Relationship types: TARGETS, ASSOCIATED_WITH, INDICATION_FOR, CONTRAINDICATION_WITH, etc.

3. Import PrimeKG

Write Cypher + batch import scripts.

Import all nodes and edges.

Ensure source provenance fields are preserved.

4. Validation

Test 10–20 graph queries (shortest-path, neighborhood, drug→gene→disease).

Ensure relationships are correct.

Deliverables

✔ PrimeKG loaded and queryable
✔ Screenshots of Neo4j graph
✔ Sample Cypher queries
✔ Short writeup for review

WEEK 2 — Dec 23 to Dec 29
⭐ Goal: Mutual Indexing Layer (Graph + Text + Embeddings) Complete
Tasks
1. Create Node Summaries

For each node: combine
name + type + textual description + top neighbors.

2. Build Embeddings

Use biomedical sentence transformer (Bio+ClinicalBERT / SciBERT).

Produce embeddings for:

Node summaries

Text chunk corpus (PrimeKG descriptions + guidelines, if any)

3. Vector Store Setup

Use FAISS or Milvus.

Store vectors: (id, embedding, type).

4. Keyword Index Setup

Use Elasticsearch/OpenSearch for BM25 retrieval.

5. Mutual Linking

For each node → store top-k (e.g., 5) nearest text chunks.

For each text chunk → store nearest graph nodes.

Deliverables

✔ Vector DB + BM25 index ready
✔ Node and document embeddings
✔ Mutual index tables (node↔chunk)
✔ Demo notebook: “Given X, which nodes/chunks are most similar?”

WEEK 3 — Dec 30 to Jan 6
⭐ Goal: Build full KAG pipeline (Planner → Retrieval → Reasoner → Verifier)
Tasks
1. Planner / Subquestion Decomposition Module

LLM prompt template to break a query into sub-questions.

2. Graph Retrieval Module

Entity grounding (exact match + fuzzy + embedding match).

K-hop Neo4j traversal with filtered relations.

3. Document Retrieval Module

Retrieve top-k text chunks using hybrid search:

BM25

Vector search

Re-ranking

4. Multi-Hop Reasoning Module

Progressive retrieval loop:
SubQ → retrieve → reason → update context → next hop.

5. Verifier

A lightweight LLM or NLI model checks if each claim is supported by retrieved evidence.

Deliverables

✔ End-to-end pipeline (console version)
✔ Ability to ask a question and get:

grounded entities

graph path

supporting text

a structured reasoning chain
✔ Notebook demo of pipeline

WEEK 4 — Jan 7 to Jan 15
⭐ Goal: Explainability UI + Evaluation + Final Report
Tasks
1. XAI / Provenance Layer

Format output as:

Final answer

Hop-by-hop reasoning

Evidence citations (Node ID + Chunk ID + Source)

Confidence score

Counter-evidence, if any

Build a minimal UI (Streamlit or simple web page) to display:

Decision tree / reasoning steps

Graph path visualization (Neo4j bloom style or NetworkX diagram)

2. Evaluation

Use 20–30 clinical-style queries.

Evaluate:

correctness

evidence precision

path validity

hallucination rate

3. Write Full Report

Introduction

Literature review

Architecture

KG construction

Mutual indexing

KAG pipeline

Experiments

Results

Limitations

Future work

4. Prepare Final PPT for Review

Clear architecture diagrams

Demo flow

Screenshots

Deliverables

✔ Streamlit demo
✔ Evaluation results
✔ Full project report (30–40 pages)
✔ Final PPT for Jan 23 review

🎯 FINAL BUFFER (Jan 15–Jan 22)

Use this week to:

Polish UI

Debug issues

Prepare demo script

Practice explanation for your review

🚀 WHAT YOU WILL DEMO ON REVIEW DAY (Jan 23)

Query:
“How is Metformin linked to lactic acidosis risk?”

System pipeline steps:

Planner breaks into subquestions

Node grounding → Drug: Metformin

Graph traversal → pathways → disease

Retrieve supporting biomedical text chunks

Multi-hop reasoning with citations

Output shown with:

Graph path visualization

Evidence snippets

Final answer + confidence

This will impress any panel.